# 🛠️ ParkingLotSystem 트러블슈팅 리포트

본 문서는 주차장 관리 시스템(ParkingLotSystem)을 개발하며 발생했던 주요 하드웨어 및 소프트웨어 통신 문제들과 그 해결 과정을 기록한 트러블슈팅 가이드입니다.

---

## 1. 스피커 출력 노이즈 및 음질 개선 (하드웨어)

### 🚨 문제 상황
마이크(INMP441)로 수음한 디지털 오디오 데이터를 MCU의 PWM(Pulse Width Modulation) 방식으로 변환하여 스피커로 바로 출력했을 때, 찢어지는 듯한 심한 노이즈가 발생하고 소리가 제대로 들리지 않았습니다. 또한 직류(DC) 성분이 스피커에 그대로 인가되어 스피커 코일에 무리가 갈 위험이 있었습니다.

### 💡 해결 방법 (저항 및 커패시터 추가)
PWM 신호는 본질적으로 사각파(Square Wave)이기 때문에 이를 부드러운 아날로그 오디오 신호로 변환해야 합니다. 이를 위해 스피커 핀(PA8)과 스피커 사이에 **RC 로우패스 필터(Low-Pass Filter) 및 커플링 커패시터** 회로를 구성했습니다.

*   **저항 (Resistor):** PWM 신호의 고주파 노이즈를 걸러내는 RC 필터 역할을 하며, 스피커로 과도한 전류가 흐르는 것을 방지(볼륨 조절 및 보호)합니다.
*   **커패시터 (Capacitor):** 직류(DC) 성분을 차단(DC Blocking)하고 오디오 신호인 교류(AC) 성분만 스피커로 통과시킵니다. 이를 통해 스피커의 떨림판이 한쪽으로 치우쳐 굳는 현상을 막고 깔끔한 소리만 출력되게 하였습니다.

---

## 2. 오디오 스트리밍 병목 현상 해결 (Baud Rate 상향)

### 🚨 문제 상황
초기 UART 통신 속도(115200 bps)에서는 텍스트 기반의 주차 감지 데이터(`$PARK...`)를 보내는 데는 문제가 없었으나, 용량이 큰 **실시간 오디오 PCM 데이터**를 보내기 시작하자 대역폭 부족으로 전송 지연이 발생했습니다. 이로 인해 DMA 수신 버퍼가 꽉 차버리는 오버런(Overrun) 에러가 발생하고, 보드가 멈추거나 오디오가 심하게 끊기는 현상이 나타났습니다.

### 💡 왜 Baud Rate를 높여야 했는가? (병목 원인 분석)
* **요구되는 데이터량:** 우리가 수음하는 오디오는 초당 16,000번(16kHz) 샘플링되며, 각 샘플은 16비트(2바이트)입니다. 즉, 1초에 최소 **32,000 바이트**의 오디오 데이터를 쉼 없이 보내야 합니다.
* **115200 bps의 한계:** UART에서 1바이트를 전송하려면 시작 비트와 종료 비트를 포함해 대략 10비트가 소요됩니다. 115200 bps는 1초에 115,200 비트를 보낸다는 뜻이므로, 초당 약 **11,520 바이트**밖에 전송하지 못합니다.
* **결과:** 오디오는 1초에 32,000바이트씩 쏟아지는데 통신선은 11,520바이트밖에 못 빼내주니, 고속도로에 병목 현상이 생기듯 버퍼(Buffer)가 터져버릴 수밖에 없었습니다.

### 💡 해결 방법 (921600 bps 고속 통신)
통신 속도를 **921600 bps**로 대폭 상향시켰습니다.
* **확보된 전송 공간:** 921600 bps는 초당 약 **92,160 바이트**를 전송할 수 있는 광활한 대역폭입니다.
* **결과:** 오디오 데이터(32,000바이트/초)를 전송하고도 초당 60,000바이트 이상의 여유 공간(대역폭)이 남게 됩니다. 이 넉넉한 공간 덕분에 수신 측 버퍼가 터질 일이 없어졌고, 남는 자투리 시간에 주차 텍스트 데이터(`$PARK`)를 끼워 넣어도 통신이 전혀 밀리지 않게 되었습니다.

---

## 3. 보드 간 통신 불가 현상 (Common GND 문제)

### 🚨 문제 상황
`ParkingDetect` 보드(TX)와 `InfoMark` 보드(RX)를 점퍼선으로 연결하고 UART 통신을 시도했으나, 데이터가 아예 수신되지 않거나 외계어 같은 깨진 쓰레기 데이터만 수신되는 현상이 발생했습니다.

### 💡 해결 방법 (GND 통일)
전압(Voltage)은 절대적인 수치가 아니라 **'어떤 지점을 0V로 볼 것인가'**에 대한 상대적인 차이입니다. 서로 다른 전원(USB 등)을 사용하는 두 보드는 각자가 생각하는 0V(GND)의 기준 높이가 다릅니다. 따라서 A보드가 3.3V(HIGH)를 보내더라도, B보드 입장에서는 기준점이 달라서 이를 1.5V나 5V로 오해할 수 있습니다.
이 문제를 해결하기 위해 **두 보드의 GND 핀을 점퍼선으로 하나 연결(Common GND)**해 주었습니다. 기준점이 똑같아지자 HIGH와 LOW 신호가 정확하게 인식되며 통신 에러가 말끔히 사라졌습니다.

---

## 4. 단일 UART 선에서 주차 데이터와 오디오 데이터 분류 (멀티플렉싱)

### 🚨 문제 상황
물리적인 통신 선(UART TX/RX)은 하나인데, 이 선을 통해 **'주차 감지 상태(텍스트)'**와 **'실시간 오디오(바이너리)'**라는 전혀 다른 성격의 두 데이터를 동시에 섞어서 보내야 했습니다. 

### 💡 해결 방법 (커스텀 패킷 프로토콜 설계)

#### 1) 왜 오디오 헤더는 `0xAA, 0x55` 2바이트를 연달아 썼을까?
오디오 PCM 데이터는 마이크가 읽어 들인 소리의 크기이므로 `0x00`부터 `0xFF`까지 무작위의 모든 숫자가 등장할 수 있습니다.
* **만약 헤더를 `0xAA` 1바이트만 쓴다면?** 사람이 말을 하다가 우연히 오디오 데이터 속에 `0xAA`와 똑같은 파형 값이 들어갈 확률이 1/256이나 됩니다. 수신 측 보드는 이를 "어? 새로운 패킷 시작인가?" 하고 착각해서 멀쩡한 오디오를 잘라먹고 파싱 에러를 일으키게 됩니다.
* **`0xAA, 0x55` 2바이트를 쓰면?** `10101010`과 `01010101`이라는 극단적으로 엇갈리는 비트 패턴이 자연스러운 오디오 파형에서 연속으로 등장할 확률은 수학적으로 1/65536로 급감합니다. 여기에 데이터 길이(Payload)와 체크섬(Checksum) 검증 로직까지 추가하여, "이것이 진짜 오디오 패킷의 시작이다!"라는 것을 100% 확신할 수 있게 만든 것입니다.

#### 2) 왜 주차 감지는 `$PARK`와 `$EVENT` 2가지를 썼을까?
효율적인 데이터 전송과 디스플레이 업데이트를 위해 성격에 따라 2가지로 분류했습니다.
* **`$PARK` (동기화 패킷):** 전체 10개 자리의 상태를 한 번에 싹 다 담아 보내는 "풀 스냅샷"입니다. 통신이 잠깐 끊겼다 연결되거나, 디스플레이 보드가 방금 켜졌을 때 기존 상태를 완벽하게 복구(Sync)하기 위해 1초마다 주기적으로 전송합니다.
* **`$EVENT` (이벤트 패킷):** 차가 자리에 막 들어오거나 빠져나간 '그 순간' 딱 한 번만 발송되는 가벼운 알림입니다. 전체 데이터를 다 보낼 필요 없이 "B1 자리에 방금 차 들어왔음!" 하고 짧게 보내서 통신 대역폭을 절약하고, LCD 화면을 지연 없이 즉각적으로 반응하게 만듭니다.
